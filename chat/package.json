{
  "name": "chat",
  "version": "1.0.0",
  "description": "- add package.json for local demo - depend only on `ipfs` - depend only on `libp2p`",
  "main": "listener.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ipfs": "^0.25.0",
  }
}
